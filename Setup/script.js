const startButton = document.getElementById('startButton');
const activationKeySelect = document.getElementById('activationKey');

startButton.addEventListener('click', () => {
  const activationKey = activationKeySelect.value;
 
});

const stopButton = document.getElementById('stopButton');
const stopKeySelect = document.getElementById('stopKey');

stopButton.addEventListener('click', () => {
  const stopKey = stopKeySelect.value;
  
});
//Баги, ошибки? Пишите в Discord @suggika